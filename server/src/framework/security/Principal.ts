
/**
 * Represents a Principal in information security - a logged-in user.
 * Extend this to create a custom principal for the use case.
 */
export type Principal = {
    id: string
    email: string
}

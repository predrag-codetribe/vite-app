{
  "name": "@app/shared",
  "version": "1.0.0",
  "devDependencies": {},
  "scripts": {
    "lint": "eslint .",
    "lint-fix": "eslint --fix .",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
